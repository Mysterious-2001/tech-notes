单仓检索：
目前的逻辑：
如果是query只带了单仓的检索->走到module.代码


如果query里部分是单仓的检索->

期望：
如果query里只带了单仓的检索：
query带了psm/repo信息：
直接走单仓检索工具
query没带psm/repo信息：
没法检索？

如果query里部分是单仓的检索：



单仓的query怎么构建：


最简单的路径：
在skill里新增一个单仓检索的scene，