工具改造目前进行到第三步：
目前我在做search_repo的改造，目前的改造方案是建立 `service/react/planner_tools/`

- `definitions.py`：现有五个工具的 strict schema
- `registry.py`：统一注册
- `validator.py`：参数、可用性、组合校验
- `compiler.py`：生成执行计划
- `handlers.py`：适配现有执行能力

1. 先迁移现有工具，保持行为不变
    
    ```
    execute_afs_commands -> afs_command
    load_file            -> skill_resource
    fork_*               -> 现有 fork 逻辑
    finish_skill         -> 现有 return_skill_result
    ```
    
    `service/tools` 下 AFS、资源读取的业务实现先不动，只加适配层。
2. 用现有五个工具跑通回归  
    确认执行结果、observation、Trace、流式事件与现在一致。
3. 再切换 ModelGateway 原生 function calling  
    删除旧 JSON action parser，让新 registry 成为唯一入口。
4. 最后接入 `search_repository`  
    此时只需注册 schema、MCP handler、availability 和组合规则，再实现与 fork 并发。

，目前做完了第二步，注意：skill_resource已经改为了load_reference,现在开始做第三步，测试不用写，只需要设计和修改代码

todo:
收口工具，把工具的实现收到一块，decision存在的必要性。