# My LLM Wiki 维护规则

这个 vault 的目标不是保存更多内容，而是减少重复思考：保留真实经历与原始上下文，沉淀可复用判断，并让重要知识能被快速找到。

## 决策顺序

处理任何笔记前，依次回答：

1. 它未来要帮助解决什么问题？
2. 它是时间记录、原始输入、领域知识、项目上下文，还是跨来源结论？
3. 现有页面能否承载？能更新旧页就不要新建近义页面。
4. 哪些原始事实必须保留，哪些结论值得进入长期知识层？
5. 这次改动是否改变了导航或结构？只有改变时才同步地图、索引和维护日志。

不要为了“整理得更整齐”而移动或改写笔记。目录和链接服务于检索与复用，不服务于形式统一。

## 当前目录模型

- `00_inbox/`：尚未判断用途的临时输入。它是中转区，不是长期存放区。
- `01_journal/`：按时间记录经历和状态。
  - `daily/YYYY/`：当前按日记录。
  - `monthly/YYYY-MM/`：按月份归档的日记。
  - `weekly/`、`yearly/`：周期复盘。
- `02_career/`：职业方向、职业认知和能力成长。
  - `growth/`：成长方法与阶段性思考。
  - `soft-skills/`：表达、沟通、组织等软能力。
- `03_professional_knowledge/`：可迁移的专业知识。
  - `agents/`：Agent、RAG、MCP、上下文工程等 AI 工程知识。
  - `backend-foundation/`：后端基础工程能力；其下按 `programming language/`、`system-design/`、`dev-practice/` 等主题组织。
- `04_projects/`：项目经验、业务规则和组织上下文。
  - `business-knowledge/internal/`：公司内部平台、术语和业务知识。
- `05_life/`：个人生活与自我理解。
  - `self/`：自传、自我认知、幸福与状态。
  - `relationships/`：人际与亲密关系。
  - `plans/`：买房等具体生活计划与决策。
  - `leisure/`：休息与娱乐。
  - `hobbies/`：英语、游戏、吉他、运动等长期爱好。
- `06_inputs/`：带来源上下文的原始输入，按 `articles/`、`books/`、`papers/`、`podcasts/` 分类。
  - `articles/ai-engineering/`：Agent、大模型与 AI 工程实践。
  - `articles/thinking-and-communication/`：思维工具、逻辑与表达。
  - `articles/career-and-life/`：职业选择、个人成长与生活判断。
- `07_maps/`：人工维护的导航层，包括个人知识系统、职业发展、技术学习、生活与思考地图。
- `_wiki/`：LLM 维护的综合知识层。
  - `index.md`：全局入口，只保留重要页面和一句话说明。
  - `concepts/`：跨来源、可复用的概念与结论。
  - `questions/`：尚未解决但值得长期追踪的问题。
  - `log.md`：影响结构或知识沉淀的维护时间线，只追加，不改写历史。
- `images/`：Obsidian 笔记引用的图片附件。

## 内容边界

### 日记与主题页

- 日记保留当时发生的事实、感受和状态，不承担长期知识整理职责。
- 日记中反复出现的模式或稳定结论，应提炼到 `02_career/`、`03_professional_knowledge/` 或 `05_life/` 的现有主题页，再从日记链接过去。
- 不替用户补写经历、动机或感受；不确定的内容保留为问题。

### 原始输入与沉淀知识

- 文章、书、论文和播客的来源、摘要与上下文放在 `06_inputs/`。
- 从输入中得到的领域结论更新到对应主题页；跨多个来源形成的稳定概念放入 `_wiki/concepts/`。
- 不把来源笔记直接改造成综合知识页，也不在两个页面维护同一份正文；通过双向链接保留证据链。

### 专业知识与项目知识

- 跨公司、跨项目可迁移的方法放入 `03_professional_knowledge/`。
- 依赖具体业务、组织、内部平台或项目背景的内容放入 `04_projects/`。
- 一篇笔记同时包含两者时，以主要用途决定归属，并链接到另一侧，不复制全文。

## 维护工作流

### 新输入

1. 用途未明确时放入 `00_inbox/`；来源明确时直接放入 `06_inputs/` 对应类型。文章按其主要复用场景进入 `articles/` 下对应主题目录，不按发布平台分类。
2. 搜索已有主题页，提炼真正新增的结论，避免创建近义页。
3. 保留从结论到来源的链接。
4. 只有新增关键入口或改变导航时，才更新相关 `07_maps/` 与 `_wiki/index.md`。
5. 只有完成知识沉淀、批量整理或结构调整时，才在 `_wiki/log.md` 顶部追加记录。

### 技术笔记

1. Agent 与 AI 工程知识放入 `03_professional_knowledge/agents/`。
2. 编程语言、系统设计、开发实践和通用后端能力放入 `03_professional_knowledge/backend-foundation/` 的对应子目录。
3. 公司内部工具、业务术语和平台规则放入 `04_projects/business-knowledge/internal/`。
4. 论文和文章的原始记录仍放入 `06_inputs/`，再链接到专业知识页。

### 日记与复盘

1. 当日日记写入 `01_journal/daily/YYYY/YYYY-MM-DD.md`。
2. 已按月归档的日记保留在 `01_journal/monthly/YYYY-MM/`，不要无理由在两个位置保留副本。
3. 周度和年度复盘只把高价值、可复用结论链接或提炼到对应主题页。

### 重命名与移动

1. 移动前先搜索同名页、旧路径和所有引用。
2. 移动后同步修复 Obsidian WikiLink、地图页和 `_wiki/index.md` 中的有效入口。
3. 不修改 `_wiki/log.md` 中的历史路径；日志记录的是当时发生的事实。
4. 不为了统一中英文命名、文件层级或格式而批量重构；只有检索困难或职责冲突时才调整结构。

## 完成标准

一次维护只有在以下条件满足时才算完成：

- 内容放置能解释“为什么在这里”。
- 原始事实、来源和用户原意没有丢失。
- 没有制造重复正文或近义孤岛页。
- 所有受影响的链接、地图和索引已同步。
- 若改动影响结构或长期知识，已在 `_wiki/log.md` 追加记录。
